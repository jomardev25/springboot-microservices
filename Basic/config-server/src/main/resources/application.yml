spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git: 
          uri: https://github.com/jomardev25/microservices-config-server
          clone-on-start: true
    
server:
  port: 9296
  
eureka:
  instance:
    preferIpAddress: true